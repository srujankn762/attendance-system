# Attendance System

A Django-based attendance management system containerized with Docker.

## Features
- Django + Gunicorn + PostgreSQL
- Docker Compose setup
- Automatic migrations on container start

## Run locally
```bash
docker-compose up --build
